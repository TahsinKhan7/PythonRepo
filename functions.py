# Functions
# functions takes an input and outputs it into something else
# takes arguments and does a specifc thing to it and outputs it
# arguments can be many things such as values, variables
# theyv can take inputs(arguments)
# run a block code
# output something or nothing

#Syntax
# definition name(arguments)

# def making_bread():
#     return 'Yummy bread'
#
# making_bread()                      # We can "call upon our function e.g.
##this prompts to excetute a givedn definied and set funciton
#

# #full name example
# def full_name(f_name, middle_n, l_name):
#     return f_name + ' ' + middle_n + ' ' + l_name
#
# print(full_name('Tahsin', 'Alam', 'Khan'))


# #Circle_area
# def find_area(radius, pi=3.14):
#     return radius*radius*pi #Note, print is not requied in functions, you can just do return
#
# find_area(int(input('What is the radius?')))
#
# # you also generally put input and print outside any function
# print(find_area(10, 3.14159))


################################################
#general format of funcitons

#def call_function(something)
    #return <apply something to your someting>
    #...
#call_function(do something)
###############################################

# def print_caps(string):
#     return string.upper()
#
# print(print_caps('I am your coach'))



